Fixed development comparison, specified before examining added-signal outcomes.
Use the same 86 eligible development records and 65 later test meals; first four earlier meals for calibration. Train on other development people only, since CGMacros does not have EDA or temperature. Reserved people remain unopened. Compare glucose, glucose+EDA, glucose+temperature, glucose+both, and a no-sensor calibration median. Use Ridge alpha=100, personal residual kernel regularization=1 as in the previous development baseline. Extract minute medians for each added channel: mean, standard deviation, and coverage in -30:0, 0:60, 60:120, 120:180 minute windows; missing summaries when coverage<50%; training-only imputation and scaling. Keep queries even when wrist data are missing. Report all methods and all macro errors; no outcome-based tuning. Verify exact glucose-only reproduction and all tolerance flags. This is a modality comparison, not an exact MealMeter replication, and summaries do not capture all high-frequency EDA dynamics.
